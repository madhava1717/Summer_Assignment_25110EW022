#include <stdio.h>
int findIntersection(int arr1[], int size1, int arr2[], int size2, int result[]) {
    int k = 0; // Tracks the size of the result array

    for (int i = 0; i < size1; i++) {
        for (int j = 0; j < size2; j++) {
            if (arr1[i] == arr2[j]) {
                
                int alreadyExists = 0;
                for (int m = 0; m < k; m++) {
                    if (result[m] == arr1[i]) {
                        alreadyExists = 1;
                        break;
                    }
                }

                
                if (!alreadyExists) {
                    result[k] = arr1[i];
                    k++;
                }
                
                break; 
            }
        }
    }
    return k; // Return the final size of the intersection array
}

int main() {
    int arr1[] = {4, 9, 5, 4};
    int arr2[] = {9, 4, 9, 8, 4};
    
    int size1 = sizeof(arr1) / sizeof(arr1[0]);
    int size2 = sizeof(arr2) / sizeof(arr2[0]);

    int maxIntersectionSize = (size1 < size2) ? size1 : size2;
    int result[maxIntersectionSize];

    int intersectionSize = findIntersection(arr1, size1, arr2, size2, result);

    printf("Intersection: [ ");
    for (int i = 0; i < intersectionSize; i++) {
        printf("%d ", result[i]);
    }
    printf("]\n");

    return 0;
}
