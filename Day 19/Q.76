#include <stdio.h>

int main() {
    int size, i, j;
    int main_diagonal_sum = 0;
    int off_diagonal_sum = 0;
    int matrix[10][10];


    printf("Enter the size of the square matrix (max 10): ");
    scanf("%d", &size);


    printf("Enter elements of the matrix:\n");
    for (i = 0; i < size; i++) {
        for (j = 0; j < size; j++) {
            printf("Element [%d][%d]: ", i, j);
            scanf("%d", &matrix[i][j]);
        }
    }
    printf("\nThe given matrix is:\n");
    for (i = 0; i < size; i++) {
        for (j = 0; j < size; j++) {
            printf("%d\t", matrix[i][j]);
        }
        printf("\n");
    }

  
    for (i = 0; i < size; i++) {
        main_diagonal_sum += matrix[i][i];                 
        off_diagonal_sum += matrix[i][size - i - 1];      
    }

    printf("\nSum of Main Diagonal elements: %d\n", main_diagonal_sum);
    printf("Sum of Off Diagonal elements: %d\n", off_diagonal_sum);

    return 0;
}
